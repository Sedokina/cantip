// Publish-to-Jira endpoint (/api/jira). Inert until the JIRA_* env vars are set
// (the button hides), so it's safe to keep. See cantip's README "Publish to Jira".
export { loader, action } from 'cantip/routes/api.jira'
